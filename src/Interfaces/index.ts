export interface IRelation {
  id: number;
  name: string;
}
export interface IPosition {
  id: number;
  name: string;
}

export interface IState {
  loading: boolean;
  error: boolean;
  success: boolean;
}
